% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vdatum.R
\name{vdatum_check_points}
\alias{vdatum_check_points}
\title{Check Which Points are Within VDatum Coverage}
\usage{
vdatum_check_points(coords, region = NULL, quiet = FALSE)
}
\arguments{
\item{coords}{Data frame with columns s_x and s_y.}

\item{region}{Character. Region to test. Default is "contiguous".}

\item{quiet}{Logical. Suppress messages? Default is FALSE.}
}
\value{
A data frame with original coordinates and a 'valid' column.
}
\description{
Tests each coordinate pair to see if it can be converted. Useful for
pre-screening points before batch conversion.
}
\examples{
\dontrun{
coords <- data.frame(
  s_x = c(-75.211, -75.46803, -150.0),
  s_y = c(36.129, 35.602986, 45.0)
)

# Check which are valid
check <- vdatum_check_points(coords)
print(check)

# Filter to only valid points
valid_coords <- coords[check$valid, ]
}

}
